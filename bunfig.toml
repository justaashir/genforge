[test]
coverage = true
# scalar threshold also enforces the (unreported) statements metric, which
# fails opaquely — enforce the two visible metrics explicitly instead
coverageThreshold = { lines = 0.85, functions = 0.85 }
coverageSkipTestFiles = true
